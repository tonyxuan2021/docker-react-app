sudo: required
services:
  - docker

before_install:
  - docker build -t tonyxuan2022/docker-react-app -f Dockerfile.dev .

script:
  - docker run -e CI=true USERNAME/docker-react-app npm run test

language: generic
